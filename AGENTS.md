# AGENTS.md

Quick-start guide for AI agents working on **NzbDav**.

## What this project is

NzbDav is a **WebDAV server** that mounts NZB documents as a virtual filesystem and streams content directly from Usenet providers — without downloading files to disk first. It exposes a **SABnzbd-compatible API** so Sonarr, Radarr, and similar tools can use it as a drop-in download client.

The shipped product is a **single Docker image** that runs two processes:

| Process | Stack | Default port | Role |
|---------|-------|--------------|------|
| Backend | .NET 10 (`backend/`) | 8080 (5000 in local dev) | WebDAV, Usenet streaming, queue/history, SAB API, SQLite DB |
| Frontend | React Router 7 + Express (`frontend/`) | 3000 | Admin UI, auth, proxies WebDAV/API to backend |

Persistent state lives under `CONFIG_PATH` (default `/config`): SQLite DB, settings, blobs.

## Repository layout

```
nzbdav/
├── backend/                 # C# ASP.NET Core app (namespace: NzbWebDAV)
│   ├── Api/
│   │   ├── Controllers/     # Admin REST API (/api/*)
│   │   └── SabControllers/  # SABnzbd-compatible API (/api?mode=*)
│   ├── Auth/                # WebDAV + API authentication
│   ├── Clients/
│   │   ├── Usenet/          # NNTP connection pool, streaming, circuit breaker
│   │   ├── RadarrSonarr/    # *Arr integration
│   │   └── Rclone/          # Rclone sidecar client
│   ├── Config/              # ConfigManager, settings persistence
│   ├── Database/            # EF Core + SQLite, migrations, DavDatabaseClient
│   ├── Queue/               # NZB queue processing, file aggregators (RAR/7z)
│   ├── Services/            # Background hosted services (health, cleanup, *Arr monitor)
│   ├── Streams/             # Low-level streaming/decryption helpers
│   ├── Tasks/               # One-off maintenance tasks
│   ├── WebDav/              # NWebDav store implementation (virtual FS)
│   ├── Websocket/           # Live updates to frontend
│   └── Program.cs           # App entry point
├── frontend/
│   ├── app/
│   │   ├── routes/          # React Router pages (queue, health, settings, explore, …)
│   │   ├── auth/            # Session auth (remix-auth)
│   │   └── clients/         # backend-client.server.ts — server-side API calls
│   ├── server/              # Express middleware (proxy, auth, websockets)
│   └── server.ts            # Dev/prod server entry
├── docs/                    # User-facing docs (setup guide)
├── Dockerfile               # Multi-stage build (frontend + backend → runtime)
├── entrypoint.sh            # Starts backend, waits for /health, starts frontend
├── CHANGELOG.md             # Auto-generated by release-please (do not edit by hand)
└── CONTRIBUTING.md          # Local dev setup (read this before running locally)
```

## Architecture (mental model)

```
Sonarr/Radarr ──► SAB API (/api?mode=addfile|queue|history|…)
                        │
                        ▼
                 QueueManager ──► UsenetStreamingClient ──► NNTP providers
                        │
                        ▼
                 DatabaseStore (WebDAV) ──► virtual files under /view, /nzbs, /content, …
                        ▲
Browser/Plex/rclone ──► Frontend :3000 ──proxy──► Backend :8080 (WebDAV + /api)
```

**Frontend proxy rule:** `frontend/server/app.ts` forwards WebDAV methods, `/api`, and mount paths to `BACKEND_URL`. Authenticated UI requests get `x-api-key` injected automatically.

**Backend startup:** `Program.cs` registers controllers, WebDAV (`DatabaseStore`), hosted services, and runs EF migrations when invoked with `--db-migration` (done automatically in Docker via `entrypoint.sh`).

## Local development

See [CONTRIBUTING.md](CONTRIBUTING.md) for full steps. Minimum env:

```bash
export CONFIG_PATH=/tmp/nzbdav-config
export FRONTEND_BACKEND_API_KEY=$(head -c 32 /dev/urandom | hexdump -ve '1/1 "%.2x"')
export BACKEND_URL=http://localhost:5000
```

```bash
# Terminal 1 — backend
cd backend && dotnet publish -c Release -o ./publish
mkdir -p "$CONFIG_PATH" && ./publish/NzbWebDAV --db-migration
./publish/NzbWebDAV

# Terminal 2 — frontend
cd frontend && npm install && npm run dev
```

Open `http://localhost:5173` (dev) or `http://localhost:3000` (production build).

**Before opening a PR (frontend changes):**

```bash
cd frontend && npm run typecheck
```

There is no automated test suite today. Verify behavior manually or with targeted checks.

## Where to change things

| Task | Start here |
|------|------------|
| SABnzbd API behavior | `backend/Api/SabControllers/` |
| Admin/settings API | `backend/Api/Controllers/` |
| WebDAV paths, streaming, archives | `backend/WebDav/`, `backend/Queue/`, `backend/Streams/` |
| Usenet connections / providers | `backend/Clients/Usenet/` |
| Sonarr/Radarr automation | `backend/Clients/RadarrSonarr/`, `backend/Services/ArrMonitoringService.cs` |
| Background jobs / cleanup | `backend/Services/`, `backend/Tasks/` |
| DB schema | `backend/Database/Migrations/` (see below) |
| UI pages / settings screens | `frontend/app/routes/` |
| Frontend ↔ backend wiring | `frontend/app/clients/backend-client.server.ts`, `frontend/server/app.ts` |
| Docker / release | `Dockerfile`, `entrypoint.sh`, `.github/workflows/` |

## Database migrations

Schema changes use **EF Core** against SQLite.

```bash
cd backend
dotnet ef migrations add Descriptive-Migration-Name
dotnet ef database update   # or: ./publish/NzbWebDAV --db-migration
```

Migration files go in `backend/Database/Migrations/`. `DavDatabaseContextFactory` exists for design-time tooling.

## Commit conventions (required)

All commits **must** follow [Conventional Commits](https://www.conventionalcommits.org/) so [release-please](https://github.com/googleapis/release-please) can build version bumps and `CHANGELOG.md` entries on merge to `main`.

### Format

```
<type>(<scope>): <short imperative description>
```

### Types

| Type | Release notes | When to use |
|------|---------------|-------------|
| `feat` | **Features** | New user-visible behavior |
| `fix` | **Bug Fixes** | Bug fixes, regressions, compatibility fixes |
| `chore` | *(usually omitted)* | Tooling, refactors, release automation, non-user-facing maintenance |

Use `fix(deps)` / `chore(deps)` for dependency bumps (Dependabot follows this).

### Scopes

Pick the **smallest accurate scope**. Scopes appear in changelog groupings when used consistently.

| Scope | Area |
|-------|------|
| `webdav` | WebDAV server, virtual FS, range requests, mount paths |
| `nntp` / `usenet` | NNTP client, providers, connection pool, circuit breaker |
| `queue` | Queue processing, NZB handling, post-processors |
| `sab` | SABnzbd-compatible API |
| `api` | Admin REST controllers under `/api` (non-SAB) |
| `db` | EF models, migrations, SQLite |
| `auth` | Authentication / authorization |
| `arr` | Radarr / Sonarr integration |
| `rclone` | Rclone client |
| `health` | Health checks, repairs, health UI/backend |
| `ui` | Frontend pages, components, styling |
| `deps` | Dependency updates (npm, NuGet, GitHub Actions) |
| `ci` | GitHub Actions workflows |
| `docs` | README, docs/, AGENTS.md |
| `docker` | Dockerfile, entrypoint, container runtime |

Scope is optional but **strongly preferred** for anything touching a specific subsystem. Omit scope only for small cross-cutting changes.

### Examples

```
feat(queue): backup incoming nzbs when enabled
fix(webdav): return 416 for range requests past content boundary
fix(nntp): skip failing providers with circuit breaker
feat(ui): add setting to schedule RemoveOrphanedFiles task
fix(deps): bump vite in the frontend vite group
chore(ci): publish pre-release images on main
```

### Do not use

- `chore(main): release …` — reserved for release-please automation
- Vague messages: `update stuff`, `fix bug`, `wip`
- Mixed unrelated changes in one commit

### Commit often

**Make small, focused commits** as you work — one logical change per commit. This keeps history reviewable, makes bisect/revert easier, and gives release-please clean entries for the changelog.

Prefer this rhythm:

1. Implement one piece of behavior
2. Commit with a properly scoped message
3. Repeat until the task is done

Do not accumulate a large uncommitted diff across unrelated areas.

## Releases

- Merging to `main` triggers **release-please** (`.github/workflows/release.yml`) which opens/updates a release PR and maintains `CHANGELOG.md` + `version.txt`.
- `feat` → minor bump; `fix` → patch bump (pre-1.0 rules in `.release-please-config.json`).
- Pre-release Docker images (`:pre-release`) build on every `main` push except release commits.
- Versioned releases publish to `ghcr.io` and `docker.io/nzbdav/nzbdav`.

## Coding guidelines

1. **Minimize scope** — smallest correct diff; don't refactor adjacent code unless needed.
2. **Match existing patterns** — one controller per action folder under `Api/`, CSS modules + `.module.css.d.ts` in frontend, `NzbWebDAV` root namespace in backend.
3. **Backend** — nullable enabled, async/await, Serilog for logging, `ConfigureAwait(false)` in library-style code.
4. **Frontend** — React Router file-based routes under `app/routes/`, server loaders/actions, Tailwind + Bootstrap, typed CSS modules.
5. **Security** — never commit secrets; WebDAV and admin APIs require auth; frontend injects API key only for authenticated sessions.
6. **Comments** — only for non-obvious logic; prefer clear naming.

## Common pitfalls

- **Env mismatch:** frontend and backend must share `CONFIG_PATH`, `FRONTEND_BACKEND_API_KEY`, and `BACKEND_URL`.
- **Proxy paths:** new WebDAV mount points must be added to the proxy allowlists in `frontend/server/app.ts` and compression skip list in `frontend/server.ts`.
- **Editing CHANGELOG.md:** it is generated — commit messages are the source of truth.
- **Breaking upgrades:** major incompatible releases may gate startup in `Program.cs` (see `UPGRADE` env var pattern).
- **No test suite:** rely on `npm run typecheck` and manual verification.

## Useful references

- [README.md](README.md) — product overview
- [CONTRIBUTING.md](CONTRIBUTING.md) — dev environment setup
- [docs/setup-guide.md](docs/setup-guide.md) — deployment / integrations
- [CHANGELOG.md](CHANGELOG.md) — what shipped in each version
